<?xml version="1.0" encoding="utf-8"?>
<!--
    Nada de esta app sale de este dispositivo, tampoco por el sistema.

    Hace falta además de `allowBackup="false"` porque desde Android 12 ese atributo dejó de cubrirlo
    todo: sigue apagando la copia en la nube, pero la **transferencia entre dispositivos** —la que
    ocurre al configurar un teléfono nuevo desde el viejo— se gobierna desde aquí. Con solo el
    atributo, el historial de escaneos viajaría igualmente en ese traspaso.

    Se excluye todo y no solo `database`. Excluir por dominios sueltos obliga a acertar con la lista
    y a revisarla cada vez que se guarde algo nuevo; excluir `root` cubre lo que haya hoy y lo que
    llegue mañana. Aquí no hay ningún dato que merezca la pena conservar entre teléfonos: el
    historial es un registro local y las preferencias son tres interruptores.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>

    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
